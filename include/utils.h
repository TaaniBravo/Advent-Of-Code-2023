#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#ifndef UTILS_H

#define UTILS_H

// void read_file(char *file_name, char *buf);
// void free_lines(char **lines, int line_count);
void read_line(char **line, char *buf_out);

#endif // !UTILS_H
